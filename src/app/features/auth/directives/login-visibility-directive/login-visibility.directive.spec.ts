import { LoginVisibilityDirective } from './login-visibility.directive';

describe('LoginVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
