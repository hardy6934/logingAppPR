import { LoginStateDirective } from './login-state.directive';

describe('LoginStateDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginStateDirective();
    expect(directive).toBeTruthy();
  });
});
